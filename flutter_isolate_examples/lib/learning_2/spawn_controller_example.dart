// SpawnControllerExample

import 'package:flutter/material.dart';

class SpawnControllerExample extends StatelessWidget {
  const SpawnControllerExample({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('New Page'),
      ),
    );
  }
}
