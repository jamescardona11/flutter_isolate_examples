import 'package:flutter/material.dart';

class DemoJsonPage extends StatelessWidget {
  const DemoJsonPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('New Page'),
      ),
    );
  }
}
